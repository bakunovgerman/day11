# ⚡ Быстрый старт

Это руководство поможет вам быстро начать работу с MCP Client для Kotlin.

## 📋 Требования

- Java 24 или выше
- Gradle (включен в проект через wrapper)
- Context7 API ключ (получите на https://context7.com)

## 🚀 Установка и запуск за 3 минуты

### Шаг 1: Клонирование и сборка

```bash
# Клонируйте репозиторий (или откройте существующий проект)
cd day11

# Сборка проекта
./gradlew build
```

### Шаг 2: Настройка API ключа

```bash
# Установите переменную окружения
export CONTEXT7_API_KEY="your_api_key_here"

# Для постоянного использования добавьте в ~/.zshrc или ~/.bashrc:
echo 'export CONTEXT7_API_KEY="your_api_key_here"' >> ~/.zshrc
```

### Шаг 3: Запуск

```bash
# Запустите основной пример
./gradlew run
```

## 📝 Ваш первый MCP клиент

Создайте файл `MyFirstClient.kt`:

```kotlin
import kotlinx.coroutines.runBlocking
import org.example.mcp.McpClient
import org.example.mcp.McpConfig

fun main() = runBlocking {
    // 1. Создаем конфигурацию
    val config = McpConfig(
        url = "https://mcp.context7.com/mcp",
        headers = mapOf(
            "CONTEXT7_API_KEY" to System.getenv("CONTEXT7_API_KEY")!!
        )
    )

    // 2. Создаем клиент
    val client = McpClient(config)

    try {
        // 3. Подключаемся
        val info = client.initialize()
        println("✓ Подключено к: ${info.serverInfo.name}")

        // 4. Получаем список инструментов
        val tools = client.listTools()
        println("✓ Доступно инструментов: ${tools.tools.size}")

        // 5. Выводим список
        tools.tools.forEach { tool ->
            println("  - ${tool.name}")
        }
    } finally {
        client.close()
    }
}
```

Запустите:

```bash
./gradlew run -PmainClass=MyFirstClientKt
```

## 🎯 Примеры использования

### Пример 1: Базовое подключение

```bash
./run-basic.sh
```

**Что делает:** Подключается к серверу, получает список инструментов, ресурсов и промптов.

### Пример 2: Вызов инструмента

```bash
./run-tool-call.sh
```

**Что делает:** Вызывает инструменты `resolve-library-id` и `query-docs` для получения информации о библиотеках.

### Пример 3: Batch операции

```bash
./run-batch.sh
```

**Что делает:** Параллельно запрашивает информацию о нескольких библиотеках (React, Vue, Angular, Svelte).

### Пример 4: Интерактивный режим

```bash
./run-interactive.sh
```

**Что делает:** Запускает интерактивную консоль для работы с MCP сервером.

## 🔧 Быстрые команды

```bash
# Сборка
./gradlew build

# Очистка
./gradlew clean

# Запуск тестов
./gradlew test

# Запуск основного примера
./gradlew run

# Запуск конкретного примера
./gradlew run -PmainClass=org.example.examples.BasicExampleKt
```

## 📚 Следующие шаги

1. Изучите [EXAMPLES.md](EXAMPLES.md) для подробных примеров
2. Прочитайте [README.md](README.md) для полной документации
3. Изучите исходный код в `src/main/kotlin/org/example/mcp/`
4. Попробуйте модифицировать примеры под свои нужды

## 💡 Полезные советы

### Совет 1: Используйте переменные окружения

```bash
# Вместо хардкода ключей в коде
export CONTEXT7_API_KEY="your_key"
```

### Совет 2: Используйте конфигурационные файлы

Создайте `mcp-config.json`:

```json
{
  "mcpServers": {
    "context7": {
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "YOUR_API_KEY"
      }
    }
  }
}
```

Загружайте конфигурацию:

```kotlin
val client = McpClientFactory.fromConfigFile("mcp-config.json", "context7")
```

### Совет 3: Используйте утилиты

```kotlin
import org.example.mcp.McpUtils

// Проверка существования инструмента
if (McpUtils.toolExists(client, "resolve-library-id")) {
    // Вызов инструмента
}

// Извлечение текста из результата
val text = McpUtils.extractText(result)
```

## ❓ Частые проблемы

### Проблема: "Connection refused"

**Решение:** Проверьте подключение к интернету и URL сервера.

### Проблема: "Unauthorized" или "Invalid API key"

**Решение:** Проверьте правильность API ключа:

```bash
echo $CONTEXT7_API_KEY
```

### Проблема: Java version

**Решение:** Убедитесь что используется Java 24+:

```bash
java -version
```

## 🆘 Помощь

Если у вас возникли проблемы:

1. Проверьте переменную `CONTEXT7_API_KEY`
2. Убедитесь что проект собран: `./gradlew build`
3. Посмотрите логи в консоли
4. Изучите примеры кода в `src/main/kotlin/org/example/examples/`

## 🎉 Готово!

Теперь вы готовы использовать MCP Client для Kotlin. Удачи! 🚀
