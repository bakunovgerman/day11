# 🔧 Устранение неполадок

## Ошибка: Parse error (-32700)

### Описание
```
MCP Error [-32700]: Parse error
```

Эта ошибка означает, что сервер не может распарсить запрос или клиент не может распарсить ответ.

### Причины

1. **Неверный формат ответа** - Сервер возвращает не JSON, а другой формат (например, HTML или SSE)
2. **Отсутствующие заголовки** - Сервер требует специфичные HTTP заголовки
3. **Неверный URL** - Адрес сервера неправильный
4. **Проблемы с API ключом** - Неверный или отсутствующий ключ

### Решение

#### Шаг 1: Включите отладочный режим

Отладочный режим уже включен по умолчанию в примерах. При запуске вы увидите:

```
DEBUG: Sending request to https://mcp.context7.com/mcp
DEBUG: Request body: {"jsonrpc":"2.0","id":"...","method":"initialize","params":{...}}
DEBUG: Response status: 200 OK
DEBUG: Response content type: application/json
DEBUG: Response body: {...}
```

#### Шаг 2: Проверьте вывод

Запустите программу и посмотрите на отладочный вывод:

```bash
./gradlew run
```

**Ищите:**

1. **Response status** - должен быть `200 OK`
   - Если `401` → Проблема с API ключом
   - Если `406` → Проблема с заголовком Accept
   - Если `404` → Неверный URL

2. **Response content type** - должен быть `application/json`
   - Если `text/html` → Сервер вернул HTML (обычно страницу ошибки)
   - Если `text/event-stream` → Сервер ожидает SSE соединение

3. **Response body** - должен начинаться с `{` (JSON)
   - Если начинается с `<` → Это HTML
   - Если начинается с `data:` → Это SSE

#### Шаг 3: Исправьте проблему

##### Если Response status: 401 Unauthorized

**Проблема:** Неверный API ключ

**Решение:**
```bash
# Проверьте ключ
echo $CONTEXT7_API_KEY

# Если пустой или неверный, обновите
export CONTEXT7_API_KEY="ваш_правильный_ключ"
```

##### Если Response status: 406 Not Acceptable

**Проблема:** Сервер не принимает наш Accept заголовок

**Решение:** Уже исправлено в коде! Заголовок `Accept: application/json, text/event-stream` добавлен.

##### Если Response content type: text/html

**Проблема:** Сервер вернул HTML страницу (обычно страницу ошибки)

**Решение:**
1. Проверьте URL сервера
2. Проверьте доступность сервера: `curl -I https://mcp.context7.com/mcp`
3. Проверьте API ключ

##### Если Response body не JSON

**Проблема:** Ответ не в формате JSON

**Решение:**
1. Посмотрите на первые символы ответа в DEBUG выводе
2. Если HTML - проверьте URL и ключ
3. Если SSE - возможно нужен другой транспорт

#### Шаг 4: Проверьте сетевое соединение

```bash
# Проверьте доступность сервера
curl -X POST https://mcp.context7.com/mcp \
  -H "Content-Type: application/json" \
  -H "Accept: application/json, text/event-stream" \
  -H "CONTEXT7_API_KEY: ваш_ключ" \
  -d '{"jsonrpc":"2.0","id":"test","method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"Test","version":"1.0"}}}'
```

Должен вернуть JSON с информацией о сервере.

## Ошибка: Not Acceptable (-32000)

### Описание
```
MCP Error [-32000]: Not Acceptable: Client must accept both application/json and text/event-stream
```

### Решение

**Уже исправлено!** Заголовок `Accept` добавлен в код:

```kotlin
header("Accept", "application/json, text/event-stream")
```

Пересоберите проект:
```bash
./gradlew clean build
```

## Ошибка: Unable to locate a Java Runtime

### Описание
```
Unable to locate a Java Runtime.
Please visit http://www.java.com for information on installing Java.
```

### Решение

Установите Java 24:

```bash
# macOS (SDKMAN - рекомендуется)
curl -s "https://get.sdkman.io" | bash
source "$HOME/.sdkman/bin/sdkman-init.sh"
sdk install java 24-open

# Проверьте
java -version
```

Подробнее: [INSTALLATION.md](INSTALLATION.md)

## Ошибка: Connection refused

### Описание
```
Connection refused
```

### Причины
1. Нет интернета
2. Сервер недоступен
3. Неверный URL

### Решение

```bash
# Проверьте интернет
ping google.com

# Проверьте сервер
curl -I https://mcp.context7.com/mcp

# Проверьте URL в коде
# Должно быть: https://mcp.context7.com/mcp
```

## Отладочный режим

### Включение

По умолчанию отладочный режим **включен** в примерах.

Для вашего кода:

```kotlin
val client = McpClient(
    config = config,
    debug = true  // Включить отладку
)
```

### Выключение

```kotlin
val client = McpClient(
    config = config,
    debug = false  // Выключить отладку
)
```

### Что показывает отладочный режим

```
DEBUG: Sending request to https://mcp.context7.com/mcp
DEBUG: Request body: {"jsonrpc":"2.0",...}
DEBUG: Response status: 200 OK
DEBUG: Response content type: application/json
DEBUG: Response body: {"jsonrpc":"2.0",...}
```

## Логирование Ktor

Для более подробных логов HTTP запросов отредактируйте `src/main/resources/logback.xml`:

```xml
<!-- Текущее (INFO) -->
<logger name="io.ktor" level="INFO" />

<!-- Подробное (DEBUG) -->
<logger name="io.ktor" level="DEBUG" />

<!-- Только ошибки (ERROR) -->
<logger name="io.ktor" level="ERROR" />
```

## Часто задаваемые вопросы

### Q: Как проверить правильность API ключа?

```bash
echo $CONTEXT7_API_KEY
# Должен показать ваш ключ
```

### Q: Где посмотреть полный запрос/ответ?

Включите отладочный режим:
```kotlin
val client = McpClient(config, debug = true)
```

И установите DEBUG уровень для Ktor в `logback.xml`:
```xml
<logger name="io.ktor" level="DEBUG" />
```

### Q: Как протестировать соединение вручную?

```bash
curl -X POST https://mcp.context7.com/mcp \
  -H "Content-Type: application/json" \
  -H "Accept: application/json, text/event-stream" \
  -H "CONTEXT7_API_KEY: ваш_ключ" \
  -d '{
    "jsonrpc": "2.0",
    "id": "test",
    "method": "initialize",
    "params": {
      "protocolVersion": "2024-11-05",
      "capabilities": {},
      "clientInfo": {
        "name": "Test",
        "version": "1.0"
      }
    }
  }'
```

### Q: Ошибка сохраняется после исправлений?

1. Очистите и пересоберите проект:
```bash
./gradlew clean build
```

2. Проверьте, что изменения сохранены в файлах

3. Перезапустите программу

## Получение помощи

1. **Включите отладочный режим** (`debug = true`)
2. **Запустите программу** и сохраните вывод
3. **Проверьте документацию:**
   - [README.md](README.md) - Основная документация
   - [INSTALLATION.md](INSTALLATION.md) - Установка
   - [EXAMPLES.md](EXAMPLES.md) - Примеры

4. **Посмотрите исходный код примеров** в `src/main/kotlin/org/example/examples/`

## Полезные команды

```bash
# Пересборка проекта
./gradlew clean build

# Запуск с отладкой
./gradlew run

# Проверка Java
java -version

# Проверка API ключа
echo $CONTEXT7_API_KEY

# Тест сервера
curl -I https://mcp.context7.com/mcp

# Просмотр логов
tail -f build/test-results/*/TEST-*.xml
```

---

**Не работает?** Откройте `START_HERE.md` и начните с начала.
